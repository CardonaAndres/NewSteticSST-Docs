events {}

http {
    # ---------------------------
    # Definición de upstreams
    # ---------------------------

    upstream auth_api {
        server auth:3001;
    }

    upstream staff_api {
        server staff:3002;   
    }

    upstream reports_manager_api {
        server reports-manager:3003;  
    }

    # ---------------------------
    # Mapeo de origenes permitidos para CORS
    # ---------------------------
 
    map $http_origin $cors_origin {
        default "";  
        "https://sst.newstetic.com" $http_origin;  # Origen permitido (frontend local)
    }

    # ---------------------------
    # Configuración del servidor
    # ---------------------------
    server {
        listen 443 ssl;              
        server_name apisst.newstetic.com;  # Nombre del servidor (host virtual)

        ssl_certificate     /etc/nginx/ssl/selfsigned.crt;
        ssl_certificate_key /etc/nginx/ssl/selfsigned.key;

        # ---------------------------
        # Configuración global de CORS
        # ---------------------------
    
        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH ,DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept, Origin, X-Requested-With" always;
        add_header Access-Control-Allow-Credentials "true" always;

        # ---------------------------
        # Manejo de peticiones preflight (OPTIONS)
        # ---------------------------
        # Retorna 204 No Content sin pasar al backend
        if ($request_method = OPTIONS) {
            return 204;
        }

        # ---------------------------
        # Location para microservicio "auth"
        # ---------------------------
        location /API-SST/v1/auth/ {
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;

            rewrite ^/API-SST/v1/auth/(.*)$ /API/v1/auth/$1 break;

            proxy_pass http://auth_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # ---------------------------
        # Location para microservicio "staff"
        # ---------------------------
        location /API-SST/v1/ {
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;

            proxy_pass http://staff_api;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # ---------------------------
        # Location para microservicio "reports-manager"
        # ---------------------------
        location /API-SST/v1/reports {
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;

            proxy_pass http://reports_manager_api;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
